
<% if current_user.id == @user.id %>
<%= form_with(model: current_user.active_blocks.build, local: true) do |f| %>
  <div><%= hidden_field_tag :blocked_id, @user.id %></div>
  <%= f.submit "Block", class: "btn btn-primary" %>
<% end %>
<% end %>
